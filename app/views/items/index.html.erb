<h1>Items</h1>

<ul data-controller="items">
  <%= render @items.select{ |item| not item.purchased } %>
</ul>

<%= render 'form', item: @item %>

<h3>Purchased (<%= @items.select{ |item| item.purchased }.count %>)</h3>
<ul data-controller="items">
  <%= render @items.select{ |item| item.purchased } %>
</ul>
